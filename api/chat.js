// chat UI that calls /api/chat
document.addEventListener('DOMContentLoaded', ()=> {
  // create the chat button + panel dynamically (or you can reuse html)
  // for simplicity we rely on forms in index.html and separate chat page
  // Here only minimal behavior if you have /api/chat backend.
  const openBtn = document.querySelector('#scq-open-btn');
  // omitted: in our index.html the chat is integrated in a panel; ensure your HTML has #scq-open-btn etc.
});
